# 会议录音转写助手 v3.1.0 依赖
# Python 3.11 required

# Web 框架
fastapi>=0.100
uvicorn>=0.20

# 音频录制
sounddevice>=0.4
numpy>=1.24

# FunASR 语音转写
funasr>=1.0
modelscope>=1.0
torch>=2.0
torchaudio>=2.0

# 拼音级热词纠正（FunASR postprocess_hotwords 依赖）
# 缺失时程序自动降级为仅解码期热词偏置，不会报错
pypinyin>=0.50
rapidfuzz>=3.0

# 讯飞 WebSocket 实时转写
websocket-client>=1.0

# 文档导出
python-docx>=1.0

# Whisper 本地转写（中英混合 / 多语种文件转写）
faster-whisper>=1.0

# Qwen3-ASR 本地转写（transformers 后端，52 语种自动检测，中英混合）
transformers>=4.51
accelerate>=0.26
librosa>=0.10
