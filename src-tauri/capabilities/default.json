{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面壳权限集：窗口由 Rust 端创建，sidecar 用 std::process 启动；托盘/单实例为纯 Rust 无需权限；通知插件开 notify 权限供 send_notification 命令与后续 sidecar 事件使用。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "notification:default",
    "notification:allow-notify"
  ]
}
